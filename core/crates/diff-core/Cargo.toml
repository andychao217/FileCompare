[package]
name = "diff-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
similar = { version = "2.6", default-features = false, features = ["inline", "bytes"] }
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"

[dev-dependencies]
criterion = "0.5"
