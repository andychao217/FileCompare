[package]
name = "diff-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
similar = { version = "2.6", features = ["inline", "bytes", "unicode"] }
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"

[dev-dependencies]
serde_json = "1.0"
criterion = "0.5"
